# 基于 MobileNetV2 的番茄叶片病害识别系统设计与实现

## 摘要

针对传统人工识别番茄叶片病害效率低、主观性强的问题，设计并实现一种基于轻量化深度学习的番茄叶片三分类识别系统。本文从PlantVillage公开数据集中筛选番茄健康叶片、番茄早疫病和番茄晚疫病三类图像，完成数据清洗、图像增强和训练集、验证集、测试集划分，采用SimpleCNN作为基线模型，采用基于ImageNet预训练权重的MobileNetV2作为最终模型。实验使用准确率、精确率、召回率、Macro-F1和混淆矩阵评价模型性能，并使用Python和Streamlit实现本地可视化识别页面。

关键词：番茄叶片；病害识别；计算机视觉；MobileNetV2；迁移学习

## 第一章 绪论

1. 研究背景与意义；
2. 番茄叶片病害识别的研究现状；
3. 深度学习在农业图像识别中的应用；
4. 本文主要研究内容与章节安排。

## 第二章 相关技术

1. 卷积神经网络基本原理；
2. 图像分类与迁移学习原理；
3. SimpleCNN基线模型；
4. MobileNetV2网络结构；
5. 图像数据增强方法；
6. 分类评价指标。

## 第三章 数据集与预处理

本文使用PlantVillage中的番茄叶片子集，研究番茄健康叶片、番茄早疫病和番茄晚疫病三个类别。三类图像总量约4,500张，按照70%、15%、15%的比例划分训练集、验证集和测试集。

预处理流程包括：

1. 按类别筛选番茄三分类图像；
2. 删除非图像文件；
3. 统一缩放为224×224像素；
4. 对训练集进行随机裁剪、翻转、旋转和颜色增强；
5. 使用ImageNet均值和标准差进行归一化；
6. 使用固定随机种子42保证实验可复现。

## 第四章 病害识别模型设计

### 4.1 SimpleCNN基线模型

使用三层卷积结构提取叶片纹理和颜色特征，通过自适应平均池化和全连接层输出三类预测结果，作为基础对比模型。

### 4.2 MobileNetV2模型

采用ImageNet预训练MobileNetV2，替换最后分类层，使输出类别数为3。模型训练使用AdamW优化器、交叉熵损失函数和CUDA自动混合精度。根据验证集Macro-F1保存最佳模型。

## 第五章 实验结果与分析

### 5.1 实验环境

| 项目 | 配置 |
|---|---|
| 操作系统 | Windows |
| 编程语言 | Python 3.9.25 |
| 深度学习框架 | PyTorch 2.5.1+cu121 |
| GPU | NVIDIA GeForce RTX 3060 Laptop GPU |
| 显存 | 6GB |
| 输入尺寸 | 224×224 |
| 随机种子 | 42 |

### 5.2 对比实验

对SimpleCNN和MobileNetV2分别进行训练，比较以下指标：

1. Accuracy；
2. Precision；
3. Recall；
4. Macro-F1；
5. 混淆矩阵；
6. 单张图片推理时间。

本次正式实验结果如下：

| 模型 | 测试集准确率 | 测试集Macro-F1 |
|---|---:|---:|
| SimpleCNN基线 | 90.10% | 88.31% |
| MobileNetV2正式模型 | 99.85% | 99.83% |

MobileNetV2在三个类别上的测试结果为：番茄早疫病F1值99.67%，番茄晚疫病F1值99.83%，番茄健康叶片F1值100.00%。

详细结果见：

- `outputs/tomato_3class/classification_report.txt`
- `outputs/tomato_3class/confusion_matrix.png`
- `outputs/tomato_3class/training_curves.png`

### 5.3 结果分析

重点分析番茄早疫病和晚疫病之间可能出现的混淆，并比较轻量化MobileNetV2与SimpleCNN在准确率、Macro-F1和模型复杂度方面的差异。

## 第六章 可视化识别系统设计

系统使用Streamlit实现，主要功能包括：

1. 上传番茄叶片图片；
2. 调用三分类MobileNetV2模型；
3. 输出番茄健康、早疫病或晚疫病；
4. 输出预测置信度；
5. 显示Top-3预测结果。

系统启动命令：

```bash
python -m streamlit run app.py
```

## 第七章 总结与展望

总结MobileNetV2在番茄叶片三分类任务上的识别效果和系统实现过程。PlantVillage图像背景较规范，可能与真实田间环境存在差异；后续可以增加真实田间图片、病斑定位和移动端部署，但这些内容不属于本阶段的必做范围。
